# Agentopia handoff

This file is the portable, version-controlled checkpoint. It must be updated at every
verified implementation boundary and before a handoff. Git records exact source changes;
this document records why they exist, how to verify them, and how to resume safely.

Machine-specific logs, generated performance evidence, Library records, and local paths
stay outside Git. `RESTART-HANDOFF.md` remains the detailed local forensic checkpoint and
is not deleted, but it is intentionally ignored because it contains local paths and test
details that should not be published.

## Current state

- Operations, Observatory, Library, Settings, secure session actions, Terminal, and the
  Windows WebView2 host are implemented.
- Library relevance defaults to 105 agent-related records; 39 excluded records remain
  recoverable in the local Library but are not committed.
- Headless application, package, JavaScript, native security, Terminal, and lifecycle
  checks pass.
- The Observatory now caches unchanged city geometry between animation frames; the
  regression failed before the fix and the complete portable suite passes afterward.
- The Windows distribution is generated by `desktop/windows/package.py` and `dotnet
  publish`; `desktop/windows/build.ps1` is the reproducible fresh-clone entrypoint that
  assembles and hash-verifies the exact manifest-owned payload. `desktop/windows/dist/`
  is a release artifact, not repository content.
- Six portable classification regressions cover revision-bound durable storage, rejected
  mutation, reload trust, strict API dispatch, bundle/revision isolation, and promotion
  isolation. They use only public production modules; private curation tools and evidence
  remain ignored.
- Terminal panes now receive a privacy-limited projection of the existing live event stream:
  exact current/source identity, working summary, approval/status, and saved-Handoff state.
  There is no second provider poller, model call, automatic retry loop, or terminal-content log.
- Natural shell exit now completes the active output channel before releasing its attachment and
  sends the WebSocket close frame before cancelling the receiver. The regression was caught by
  the real integration gate; the fresh 1/2/8-pane rerun passed with zero owned processes after
  each scenario.
- The user-facing product, desktop title, package actions, and release executable are now Agentopia.
  Existing `%LOCALAPPDATA%\AgentFoundry` data, WebView profiles, internal API headers/namespaces, and
  Agent Foundry v1 packages remain compatible and require no migration.
- Git was initialized on `main`. The first verified source checkpoint is commit `9f27a47`
  (`Initial Agent Foundry implementation`). Generated/private files were excluded before
  staging, staged names were reviewed, and the committed portable suite passed.

## Product decisions that must survive a handoff

- Provider-neutral observation is the core. Harness, model provider, and model are separate
  identities; Codex is the only fully implemented adapter today.
- Claude Code, Pi Agent, and Hermes appear only as honest unavailable placeholders until
  real adapters exist. Generic JSON remains read-only.
- Session instructions are explicit, capability-checked, confirmed actions. Reading local
  session files never becomes a control channel, and live Send is never tested implicitly.
- Terminal panes use real native processes with bounded buffers and lifecycle ownership.
  WSL, elevation, PATH-based executable discovery, shell argument strings, and detached
  descendants are excluded from the contained V1 security model.
- Custom shells require an absolute executable path and exact SHA-256 in local
  `terminal-profiles.json`; that file is never committed.
- Skills, workflows, templates, and handoffs are ordinary versioned files. No database or
  automatic self-edit/promotion loop is part of the current product.
- Existing user projects and skills are read-only sources. Agentopia may catalog them
  but must not rewrite them.
- The Observatory must communicate real work; decorative features that do not improve
  orientation, drill-down, or actionability should be removed or simplified.

## Pending verification

- Active, foreground Observatory attempt 09 passed the revised workload-specific gate:
  14.5815% of one core, 683,569,152 bytes summed working set (<700,000,000),
  448,421,888 private bytes (<450,000,000), 303 draw heartbeats, and zero long tasks.
  Cleanup was clean with zero survivors. Its exact release hashes were manifest
  `84f4bd762c9e734d69caa0c11f9c974babc42392748da2fc3787aaebaa6bb2a0`, executable
  `f5e43db34217077efa779d1546deba004de3e5c3081dc74c7035209fe62c6e1a`, and host DLL
  `2e61d40422914ac3becd8a734bdba8ef4c8c868aee008cb6d816d27e5cb5a2be`.
  The active limits remain provisional and are not statistically validated; the idle
  Operations <600,000,000-byte gate is unchanged.
- The current Agentopia package has manifest SHA-256
  `fa7a260e71c4fdcd799e0ae56ee9bcffb6e411e1f30d45cbabb3be2e6ead2bc6`; `Agentopia.exe` is
  `67dc37ca8c8dc3fc0e4de9d443267b35fbfcaa566d1762693a85f534bede0d57` and `Agentopia.dll` is
  `73639c510493501f06851cb6965912f202fb42642a56dd3b034e8b0dc07772b1`. Its hidden desktop
  lifecycle/performance run passed with both WebViews stabilized, 0.0195% machine CPU,
  883,339,264 bytes working set, 457,490,432 private bytes, 29,679,616 bytes bounded post-warm
  growth, negative steady-state memory growth, stable process identities across both repeated
  cycles, and zero owned processes after stop. Attempt 09 does not certify this
  exact binary's visible rendering.
- Live Send and real provider launches remain deliberate manual tests and require separate
  confirmation.
- Pixel-perfect visual polish remains deferred to a later design pass.

## Resume

1. Read this file, `README.md`, `docs/CONTRACT.md`, and `docs/HELP.md`.
2. Confirm `git status --short` is understood before editing; never discard unknown local
   work or ignored Library/session data.
3. Run the portable suites below.
4. To restore a desktop release from a fresh clone, run `& .\desktop\windows\build.ps1`.
   It packages the pinned stage, performs a locked self-contained publish, copies the exact
   manifest-owned payload, and fails on any missing, extra, or hash-mismatched payload file.
5. Inspect ignored local `RESTART-HANDOFF.md` and retained evidence only when diagnosing a
   machine-specific result; do not add them to Git.
6. Continue from **Pending verification**, recording a new commit and updating this file at
   the next verified boundary.

Run the normal headless suites before changing behavior:

```powershell
py -3 -B -m unittest discover -q
py -3 -B desktop/windows/test_package.py
node test_operations.cjs
node test_observatory.cjs
node test_foundry_ui.cjs
node desktop/windows/terminal/test_terminal.cjs
dotnet run --project desktop/windows/functional-check/functional-check.csproj -c Release --no-restore
dotnet run --project desktop/windows/terminal-check/terminal-check.csproj -c Release --no-restore
# Run the Windows native and integration checkers as their direct DLLs; see their reports for commands.
dotnet run --project desktop/windows/webview-check/webview-check.csproj -c Release --no-restore -- --check-guards
```

Never commit local Library data, provider credentials, WebView profiles, terminal profile
configuration, captured session logs, or generated test evidence.

## Recovery limits

A GitHub clone restores product source, tests, portable examples, and this handoff. It does
not restore the user's local Library, imported records, provider/session logs, credentials,
terminal profiles, generated Windows distribution, or machine-specific evidence. Back up
those separately and privately when they become valuable; never solve that by committing
them to the source repository.
